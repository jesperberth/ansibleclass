---
- hosts: windowsservers

  vars_prompt:
    - name: password
      prompt: Password for safemode?

  tasks:
    - name: Configure AD
      include_role:
        name: active-directory-domain
      vars:
        domain: "testdomain.local"
        safemodepw: "{{ password }}"
